==== Allocation.php ====
allocations テーブルの 1 件のレコードは、t_order_details テーブルの特定の 1 件のレコードに属します（外部キーは allocations 側にあります）。
allocations テーブルの 1 件のレコードは、t_purchase_orders テーブルの特定の 1 件のレコードに属します（外部キーは allocations 側にあります）。
allocations テーブルの 1 件のレコードは、t_estimates テーブルの特定の 1 件のレコードに属します（外部キーは allocations 側にあります）。
allocations テーブルの 1 件のレコードは、t_arrivals テーブルの特定の 1 件のレコードに属します（外部キーは allocations 側にあります）。
allocations テーブルの 1 件のレコードは、t_configuration_part_details テーブルの特定の 1 件のレコードに属します（外部キーは allocations 側にあります）。
allocations テーブルの 1 件のレコードは、t_shipping_details テーブルの特定の 1 件のレコードに属します（外部キーは allocations 側にあります）。

==== Cache.php ====

==== CacheLock.php ====

==== ConfigurationPart.php ====
configuration_parts テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは configuration_parts 側にあります）。

==== EmployeeDepartment.php ====
employee_departments テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは employee_departments 側にあります）。
employee_departments テーブルの 1 件のレコードは、m_departments テーブルの特定の 1 件のレコードに属します（外部キーは employee_departments 側にあります）。

==== EmployeeMaintenanceWork.php ====
employee_maintenance_works テーブルの 1 件のレコードは、t_maintenance_works テーブルの特定の 1 件のレコードに属します（外部キーは employee_maintenance_works 側にあります）。
employee_maintenance_works テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは employee_maintenance_works 側にあります）。
employee_maintenance_works テーブルの 1 件のレコードは、m_company_cars テーブルの特定の 1 件のレコードに属します（外部キーは employee_maintenance_works 側にあります）。

==== HasAuthorBaseAuthenticatableModel.php ====
不明なリレーション種別です。
不明なリレーション種別です。

==== HasAuthorBaseModel.php ====
不明なリレーション種別です。
不明なリレーション種別です。

==== Job.php ====

==== JobBatch.php ====

==== LogEmail.php ====
log_emails テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは log_emails 側にあります）。
log_emails テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは log_emails 側にあります）。

==== LogIeyasu.php ====

==== LogSmartmat.php ====

==== MAccountType.php ====
m_account_types テーブルの 1 件のレコードは、log_emails テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_billing_statuses テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_boxes テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_car_types テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_check_sheet_bases テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_check_sheet_details テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_companies テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_company_cars テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_customer_departments テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_customer_field_types テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_customer_people テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_delivery_destinations テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_fixed_value_names テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_holidays テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_ieyasu_settings テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_industries テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_liquid_types テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_makers テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_menus テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_notices テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_part_categories テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_parts テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_process_patterns テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_processes テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_production_numbers テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_racks テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_ships テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_smartmats テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_arrivals テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_billing_details テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_billings テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_charter_truck_estimate_details テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_charter_truck_estimates テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_charter_truck_request_arrangement_details テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_charter_truck_request_arrangements テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_charter_truck_request_groups テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_charter_truck_request_transport_companies テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_charter_truck_requests テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_check_sheet_approval_requests テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_check_sheet_record_details テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_check_sheet_records テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_configuration_part_details テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_contract_files テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_order_shippings テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_contracts テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_estimate_supplier_deliveries テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_design_process_groups テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_design_process_orders テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_estimate_deliveries テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_drawing_approval_request_histories テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_drawing_approval_requests テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_employee_schedules テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_estimate_suppliers テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_estimates テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_in_house_productions テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_inventories テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_location_part_replenishment_points テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_machine_returns テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_maintenance_work_record_pause_times テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_maintenance_work_records テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_maintenance_works テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_maintenances テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_notices テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_order_details テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_order_other_items テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_orders テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_part_files テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_picking_boxes テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_pickings テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_number_histories テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_picking_part_records テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_picking_parts テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_process_change_logs テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_process_changes テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_process_groups テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_process_order_group_record_pause_times テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_process_order_group_records テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_process_order_groups テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_process_orders テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_production_rejects テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_purchase_order_details テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_purchase_order_groups テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_purchase_orders テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_rack_part_stock_histories テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_rack_part_stocks テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_shipping_details テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_shipping_invoice_numbers テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_shipping_other_items テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_shipping_picking_part_records テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_shipping_picking_parts テーブルの複数のレコードを持つことができます。
m_account_types テーブルの 1 件のレコードは、t_shippings テーブルの複数のレコードを持つことができます。

==== MArrivalStatus.php ====
m_arrival_statuses テーブルの 1 件のレコードは、t_arrivals テーブルの複数のレコードを持つことができます。

==== MArrivalType.php ====
m_arrival_types テーブルの 1 件のレコードは、t_arrivals テーブルの複数のレコードを持つことができます。

==== MAssignedWorkType.php ====
m_assigned_work_types テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MBankDepositType.php ====
m_bank_deposit_types テーブルの 1 件のレコードは、m_our_company_settings テーブルの複数のレコードを持つことができます。

==== MBillingStatus.php ====
m_billing_statuses テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_billing_statuses 側にあります）。
m_billing_statuses テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_billing_statuses 側にあります）。
m_billing_statuses テーブルの 1 件のレコードは、t_billing_details テーブルの複数のレコードを持つことができます。

==== MBox.php ====
m_boxes テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_boxes 側にあります）。
m_boxes テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_boxes 側にあります）。
m_boxes テーブルの 1 件のレコードは、t_picking_boxes テーブルの複数のレコードを持つことができます。

==== MCarType.php ====
m_car_types テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_car_types 側にあります）。
m_car_types テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_car_types 側にあります）。
m_car_types テーブルの 1 件のレコードは、group_m_transport_companies テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_car_types テーブルの 1 件のレコードは、group_t_charter_truck_request_groups テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_car_types テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_car_types テーブルの 1 件のレコードは、t_charter_truck_requests テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_car_types テーブルの 1 件のレコードは、m_delivery_destinations テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_car_types テーブルの 1 件のレコードは、t_charter_truck_estimates テーブルの複数のレコードを持つことができます。
m_car_types テーブルの 1 件のレコードは、t_charter_truck_request_arrangements テーブルの複数のレコードを持つことができます。
m_car_types テーブルの 1 件のレコードは、t_machine_returns テーブルの複数のレコードを持つことができます。

==== MCharterTruckRequestArrangementStatus.php ====
m_charter_truck_request_arrangement_statuses テーブルの 1 件のレコードは、t_charter_truck_request_arrangements テーブルの複数のレコードを持つことができます。

==== MCharterTruckRequestEntryStatus.php ====
m_charter_truck_request_entry_statuses テーブルの 1 件のレコードは、t_charter_truck_request_transport_companies テーブルの複数のレコードを持つことができます。

==== MCharterTruckRequestOrderStatus.php ====
m_charter_truck_request_order_statuses テーブルの 1 件のレコードは、t_charter_truck_request_transport_companies テーブルの複数のレコードを持つことができます。

==== MCharterTruckRequestStatus.php ====
m_charter_truck_request_statuses テーブルの 1 件のレコードは、t_charter_truck_request_groups テーブルの複数のレコードを持つことができます。

==== MCheckSheetApprovalRequestStatus.php ====
m_check_sheet_approval_request_statuses テーブルの 1 件のレコードは、t_check_sheet_approval_requests テーブルの複数のレコードを持つことができます。

==== MCheckSheetBase.php ====
m_check_sheet_bases テーブルの 1 件のレコードは、m_processes テーブルの特定の 1 件のレコードに属します（外部キーは m_check_sheet_bases 側にあります）。
m_check_sheet_bases テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_check_sheet_bases 側にあります）。
m_check_sheet_bases テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_check_sheet_bases 側にあります）。
m_check_sheet_bases テーブルの 1 件のレコードは、m_check_sheet_details テーブルの複数のレコードを持つことができます。
m_check_sheet_bases テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは m_check_sheet_bases 側にあります）。

==== MCheckSheetDetail.php ====
m_check_sheet_details テーブルの 1 件のレコードは、m_check_sheet_bases テーブルの特定の 1 件のレコードに属します（外部キーは m_check_sheet_details 側にあります）。
m_check_sheet_details テーブルの 1 件のレコードは、m_check_sheet_detail_types テーブルの特定の 1 件のレコードに属します（外部キーは m_check_sheet_details 側にあります）。
m_check_sheet_details テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_check_sheet_details 側にあります）。
m_check_sheet_details テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_check_sheet_details 側にあります）。
m_check_sheet_details テーブルの 1 件のレコードは、t_check_sheet_record_details テーブルの複数のレコードを持つことができます。

==== MCheckSheetDetailType.php ====
m_check_sheet_detail_types テーブルの 1 件のレコードは、m_check_sheet_details テーブルの複数のレコードを持つことができます。
m_check_sheet_detail_types テーブルの 1 件のレコードは、t_check_sheet_record_details テーブルの複数のレコードを持つことができます。

==== MCheckSheetStatus.php ====
m_check_sheet_statuses テーブルの 1 件のレコードは、t_check_sheet_records テーブルの複数のレコードを持つことができます。

==== MCompany.php ====
m_companies テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_companies 側にあります）。
m_companies テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_companies 側にあります）。
m_companies テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードを持つことができます。

==== MCompanyCar.php ====
m_company_cars テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_company_cars 側にあります）。
m_company_cars テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_company_cars 側にあります）。
m_company_cars テーブルの 1 件のレコードは、t_maintenance_works テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_company_cars テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_company_cars テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは m_company_cars 側にあります）。

==== MContractUpdateDivision.php ====
m_contract_update_divisions テーブルの 1 件のレコードは、t_billings テーブルの複数のレコードを持つことができます。

==== MCustomer.php ====
m_customers テーブルの 1 件のレコードは、m_month_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、m_companies テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、m_honorifics テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、m_prefectures テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、m_tax_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、m_customer_departments テーブルの複数のレコードを持つことができます。
m_customers テーブルの 1 件のレコードは、m_customer_persons テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_customers テーブルの 1 件のレコードは、m_customer_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_customers テーブルの 1 件のレコードは、m_delivery_destinations テーブルの複数のレコードを持つことができます。
m_customers テーブルの 1 件のレコードは、m_ships テーブルの複数のレコードを持つことができます。
m_customers テーブルの 1 件のレコードは、m_industries テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_customers テーブルの 1 件のレコードは、m_invoice_send_methods テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、m_customer_banks テーブルの複数のレコードを持つことができます。
m_customers テーブルの 1 件のレコードは、m_external_accounts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_customers テーブルの 1 件のレコードは、invoice_send_prefectures テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、under_transfer_due_months テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、over_transfer_due_months テーブルの特定の 1 件のレコードに属します（外部キーは m_customers 側にあります）。
m_customers テーブルの 1 件のレコードは、ship_owner_contracts テーブルの複数のレコードを持つことができます。
m_customers テーブルの 1 件のレコードは、engine_maker_contracts テーブルの複数のレコードを持つことができます。
m_customers テーブルの 1 件のレコードは、end_user_contracts テーブルの複数のレコードを持つことができます。
m_customers テーブルの 1 件のレコードは、assigned_m_customer_persons テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_customers テーブルの 1 件のレコードは、field_m_customer_persons テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_customers テーブルの 1 件のレコードは、invoice_to_m_customer_persons テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MCustomerBank.php ====
m_customer_banks テーブルの 1 件のレコードは、m_customers テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_banks 側にあります）。
m_customer_banks テーブルの 1 件のレコードは、m_bank_deposit_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_banks 側にあります）。
m_customer_banks テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_banks 側にあります）。
m_customer_banks テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_banks 側にあります）。

==== MCustomerDepartment.php ====
m_customer_departments テーブルの 1 件のレコードは、m_customers テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_departments 側にあります）。
m_customer_departments テーブルの 1 件のレコードは、m_customer_field_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_departments 側にあります）。
m_customer_departments テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_departments 側にあります）。
m_customer_departments テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_departments 側にあります）。
m_customer_departments テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_customer_departments テーブルの 1 件のレコードは、m_customer_people テーブルの複数のレコードを持つことができます。

==== MCustomerFieldType.php ====
m_customer_field_types テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_field_types 側にあります）。
m_customer_field_types テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_field_types 側にあります）。
m_customer_field_types テーブルの 1 件のレコードは、m_customer_departments テーブルの複数のレコードを持つことができます。

==== MCustomerPerson.php ====
m_customer_people テーブルの 1 件のレコードは、m_customer_departments テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_people 側にあります）。
m_customer_people テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_people 側にあります）。
m_customer_people テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_customer_people 側にあります）。
m_customer_people テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_customer_people テーブルの 1 件のレコードは、t_orders テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MCustomerType.php ====
m_customer_types テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_customer_types テーブルの 1 件のレコードは、t_billings テーブルの複数のレコードを持つことができます。
m_customer_types テーブルの 1 件のレコードは、t_orders テーブルの複数のレコードを持つことができます。

==== MDeliveryDay.php ====
m_delivery_days テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_days 側にあります）。
m_delivery_days テーブルの 1 件のレコードは、m_prefectures テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_days 側にあります）。
m_delivery_days テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_days 側にあります）。
m_delivery_days テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_days 側にあります）。

==== MDeliveryDestination.php ====
m_delivery_destinations テーブルの 1 件のレコードは、m_delivery_destination_types テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_destinations 側にあります）。
m_delivery_destinations テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_destinations 側にあります）。
m_delivery_destinations テーブルの 1 件のレコードは、m_customers テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_destinations 側にあります）。
m_delivery_destinations テーブルの 1 件のレコードは、m_honorifics テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_destinations 側にあります）。
m_delivery_destinations テーブルの 1 件のレコードは、m_prefectures テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_destinations 側にあります）。
m_delivery_destinations テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_destinations 側にあります）。
m_delivery_destinations テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_destinations 側にあります）。
m_delivery_destinations テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_destinations 側にあります）。
m_delivery_destinations テーブルの 1 件のレコードは、m_car_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_delivery_destinations テーブルの 1 件のレコードは、t_arrivals テーブルの複数のレコードを持つことができます。
m_delivery_destinations テーブルの 1 件のレコードは、t_estimates テーブルの複数のレコードを持つことができます。
m_delivery_destinations テーブルの 1 件のレコードは、t_order_details テーブルの複数のレコードを持つことができます。
m_delivery_destinations テーブルの 1 件のレコードは、t_orders テーブルの複数のレコードを持つことができます。
m_delivery_destinations テーブルの 1 件のレコードは、t_shippings テーブルの複数のレコードを持つことができます。
m_delivery_destinations テーブルの 1 件のレコードは、ship_from_t_shippings テーブルの複数のレコードを持つことができます。

==== MDeliveryDestinationType.php ====
m_delivery_destination_types テーブルの 1 件のレコードは、m_delivery_destinations テーブルの複数のレコードを持つことができます。
m_delivery_destination_types テーブルの 1 件のレコードは、t_shippings テーブルの複数のレコードを持つことができます。

==== MDeliveryMethod.php ====
m_delivery_methods テーブルの 1 件のレコードは、t_estimates テーブルの複数のレコードを持つことができます。
m_delivery_methods テーブルの 1 件のレコードは、t_purchase_order_details テーブルの複数のレコードを持つことができます。

==== MDeliverySpecificTime.php ====
m_delivery_specific_times テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは m_delivery_specific_times 側にあります）。
m_delivery_specific_times テーブルの 1 件のレコードは、t_order_shippings テーブルの複数のレコードを持つことができます。

==== MDepartment.php ====
m_departments テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは m_departments 側にあります）。
m_departments テーブルの 1 件のレコードは、m_department_types テーブルの特定の 1 件のレコードに属します（外部キーは m_departments 側にあります）。
m_departments テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_departments テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_departments 側にあります）。
m_departments テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_departments 側にあります）。

==== MDepartmentType.php ====
m_department_types テーブルの 1 件のレコードは、m_departments テーブルの複数のレコードを持つことができます。

==== MDesignProcessGroupDrawingApprovalStatus.php ====

==== MDesignProcessOrderStatus.php ====
m_design_process_order_statuses テーブルの 1 件のレコードは、t_design_process_orders テーブルの複数のレコードを持つことができます。

==== MDrawingApprovalStatus.php ====
m_drawing_approval_statuses テーブルの 1 件のレコードは、t_drawing_approval_request_histories テーブルの複数のレコードを持つことができます。
m_drawing_approval_statuses テーブルの 1 件のレコードは、t_drawing_approval_requests テーブルの複数のレコードを持つことができます。

==== MEmployee.php ====
m_employees テーブルの 1 件のレコードは、m_groups テーブルの特定の 1 件のレコードに属します（外部キーは m_employees 側にあります）。
m_employees テーブルの 1 件のレコードは、m_printers テーブルの特定の 1 件のレコードに属します（外部キーは m_employees 側にあります）。
m_employees テーブルの 1 件のレコードは、m_assigned_work_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、m_departments テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、m_features テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、m_privilege_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、t_design_process_orders テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、t_employee_schedules テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、t_maintenances テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、t_maintenance_works テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、m_company_cars テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、t_maintenance_work_records テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、t_notices テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、t_production_process_orders テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_employees テーブルの 1 件のレコードは、approved_production_orders テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、t_purchase_orders テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、t_purchase_order_details テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、t_pickings テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、t_orders テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、t_estimates テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、t_contracts テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、m_our_company_settings テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、m_ieyasu_settings テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、m_check_sheet_bases テーブルの複数のレコードを持つことができます。
m_employees テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_employees 側にあります）。
m_employees テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_employees 側にあります）。

==== MEquipment.php ====
m_equipment テーブルの 1 件のレコードは、t_contracts テーブルの複数のレコードを持つことができます。
m_equipment テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_equipment 側にあります）。
m_equipment テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_equipment 側にあります）。

==== MEstimateRequestStatus.php ====

==== MEstimateStatus.php ====
m_estimate_statuses テーブルの 1 件のレコードは、t_estimates テーブルの複数のレコードを持つことができます。

==== MExternalAccount.php ====
m_external_accounts テーブルの 1 件のレコードは、m_external_divisions テーブルの特定の 1 件のレコードに属します（外部キーは m_external_accounts 側にあります）。
m_external_accounts テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_external_accounts テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_external_accounts テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_external_accounts テーブルの 1 件のレコードは、t_external_account_records テーブルの複数のレコードを持つことができます。
m_external_accounts テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_external_accounts 側にあります）。
m_external_accounts テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_external_accounts 側にあります）。

==== MExternalAccountStatus.php ====
m_external_account_statuses テーブルの 1 件のレコードは、m_external_accounts テーブルの複数のレコードを持つことができます。

==== MExternalDivision.php ====
m_external_divisions テーブルの 1 件のレコードは、m_external_accounts テーブルの複数のレコードを持つことができます。

==== MFactoryCategory.php ====
m_factory_categories テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_factory_categories 側にあります）。
m_factory_categories テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_factory_categories 側にあります）。
m_factory_categories テーブルの 1 件のレコードは、t_contracts テーブルの複数のレコードを持つことができます。

==== MFeature.php ====
m_features テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_features テーブルの 1 件のレコードは、employee_m_privilege_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_features テーブルの 1 件のレコードは、m_privilege_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MFixedValueCategory.php ====
m_fixed_value_categories テーブルの 1 件のレコードは、m_fixed_value_names テーブルの複数のレコードを持つことができます。

==== MFixedValueName.php ====
m_fixed_value_names テーブルの 1 件のレコードは、m_fixed_value_categories テーブルの特定の 1 件のレコードに属します（外部キーは m_fixed_value_names 側にあります）。
m_fixed_value_names テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_fixed_value_names 側にあります）。
m_fixed_value_names テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_fixed_value_names 側にあります）。

==== MGroup.php ====
m_groups テーブルの 1 件のレコードは、m_notices テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_groups テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードを持つことができます。
m_groups テーブルの 1 件のレコードは、m_process_types テーブルの特定の 1 件のレコードに属します（外部キーは m_groups 側にあります）。

==== MHoliday.php ====
m_holidays テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_holidays 側にあります）。
m_holidays テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_holidays 側にあります）。

==== MHonorific.php ====
m_honorifics テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードを持つことができます。
m_honorifics テーブルの 1 件のレコードは、m_delivery_destinations テーブルの複数のレコードを持つことができます。

==== MIeyasuSetting.php ====
m_ieyasu_settings テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは m_ieyasu_settings 側にあります）。
m_ieyasu_settings テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_ieyasu_settings 側にあります）。
m_ieyasu_settings テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_ieyasu_settings 側にあります）。

==== MInHouseProductionStatus.php ====
m_in_house_production_statuses テーブルの 1 件のレコードは、t_in_house_productions テーブルの複数のレコードを持つことができます。

==== MInHouseProductionWorkStatus.php ====

==== MIndustry.php ====
m_industries テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_industries 側にあります）。
m_industries テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_industries 側にあります）。
m_industries テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MInstructionWork.php ====
m_instruction_works テーブルの 1 件のレコードは、t_in_house_productions テーブルの複数のレコードを持つことができます。

==== MInvoiceSendMethod.php ====
m_invoice_send_methods テーブルの 1 件のレコードは、t_billings テーブルの複数のレコードを持つことができます。

==== MLiquidType.php ====
m_liquid_types テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_liquid_types 側にあります）。
m_liquid_types テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_liquid_types 側にあります）。
m_liquid_types テーブルの 1 件のレコードは、t_contracts テーブルの複数のレコードを持つことができます。

==== MLocation.php ====
m_locations テーブルの 1 件のレコードは、m_location_types テーブルの特定の 1 件のレコードに属します（外部キーは m_locations 側にあります）。
m_locations テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、m_delivery_destinations テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、m_departments テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、m_production_numbers テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、m_racks テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、m_smartmats テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、t_arrivals テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、t_contracts テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、t_location_part_replenishment_points テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、t_machine_returns テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、t_orders テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、t_pickings テーブルの複数のレコードを持つことができます。
m_locations テーブルの 1 件のレコードは、m_prefectures テーブルの特定の 1 件のレコードに属します（外部キーは m_locations 側にあります）。
m_locations テーブルの 1 件のレコードは、m_honorifics テーブルの特定の 1 件のレコードに属します（外部キーは m_locations 側にあります）。
m_locations テーブルの 1 件のレコードは、m_bank_deposit_types テーブルの特定の 1 件のレコードに属します（外部キーは m_locations 側にあります）。

==== MLocationType.php ====
m_location_types テーブルの 1 件のレコードは、m_locations テーブルの複数のレコードを持つことができます。

==== MMachineMaintenanceStatus.php ====
m_machine_maintenance_statuses テーブルの 1 件のレコードは、m_production_numbers テーブルの複数のレコードを持つことができます。

==== MMachineStatus.php ====
m_machine_statuses テーブルの 1 件のレコードは、m_production_numbers テーブルの複数のレコードを持つことができます。

==== MMaintenanceType.php ====
m_maintenance_types テーブルの 1 件のレコードは、t_maintenances テーブルの複数のレコードを持つことができます。
m_maintenance_types テーブルの 1 件のレコードは、t_maintenance_works テーブルの複数のレコードを持つことができます。

==== MMaintenanceWorkStatus.php ====
m_maintenance_work_statuses テーブルの 1 件のレコードは、t_maintenance_works テーブルの複数のレコードを持つことができます。

==== MMaker.php ====
m_makers テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_makers 側にあります）。
m_makers テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_makers 側にあります）。
m_makers テーブルの 1 件のレコードは、m_parts テーブルの複数のレコードを持つことができます。

==== MMenu.php ====
m_menus テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_menus 側にあります）。
m_menus テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_menus 側にあります）。

==== MMonthRangeType.php ====

==== MMonthType.php ====

==== MNotice.php ====
m_notices テーブルの 1 件のレコードは、m_notice_types テーブルの特定の 1 件のレコードに属します（外部キーは m_notices 側にあります）。
m_notices テーブルの 1 件のレコードは、m_notice_levels テーブルの特定の 1 件のレコードに属します（外部キーは m_notices 側にあります）。
m_notices テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_notices 側にあります）。
m_notices テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_notices 側にあります）。
m_notices テーブルの 1 件のレコードは、m_groups テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MNoticeLevel.php ====
m_notice_levels テーブルの 1 件のレコードは、m_notices テーブルの複数のレコードを持つことができます。

==== MNoticeType.php ====
m_notice_types テーブルの 1 件のレコードは、m_notices テーブルの複数のレコードを持つことができます。

==== MNumbering.php ====
m_numberings テーブルの 1 件のレコードは、m_numbering_types テーブルの特定の 1 件のレコードに属します（外部キーは m_numberings 側にあります）。

==== MNumberingType.php ====
m_numbering_types テーブルの 1 件のレコードは、m_numberings テーブルの 1 件のレコードを持ちます。

==== MOrderStatus.php ====
m_order_statuses テーブルの 1 件のレコードは、t_orders テーブルの複数のレコードを持つことができます。

==== MOurCompanySetting.php ====
m_our_company_settings テーブルの 1 件のレコードは、m_bank_deposit_types テーブルの特定の 1 件のレコードに属します（外部キーは m_our_company_settings 側にあります）。
m_our_company_settings テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは m_our_company_settings 側にあります）。

==== MOverhaulType.php ====
m_overhaul_types テーブルの 1 件のレコードは、t_order_other_items テーブルの複数のレコードを持つことができます。

==== MPage.php ====
m_pages テーブルの 1 件のレコードは、m_work_types テーブルの特定の 1 件のレコードに属します（外部キーは m_pages 側にあります）。

==== MPart.php ====
m_parts テーブルの 1 件のレコードは、m_part_category_1s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_category_2s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_category_3s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_category_4s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_category_5s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_category_6s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_category_7s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_category_8s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_category_9s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_category_10s テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_makers テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_unit_types テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_lot_unit_types テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_parts 側にあります）。
m_parts テーブルの 1 件のレコードは、m_part_attributes テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、parent_parts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、child_parts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、m_production_numbers テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、m_smartmats テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_arrivals テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_configuration_part_details テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_estimates テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_inventories テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_in_house_productions テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_location_part_replenishment_points テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、get_kakogawa_location_part_replenishment_points テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_order_details テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_part_files テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_purchase_order_details テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_purchase_orders テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_rack_part_stock_histories テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_rack_part_stocks テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_shipping_picking_parts テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、t_production_picking_parts テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、w_arrivals テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、w_production_pickings テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、w_shipping_pickings テーブルの複数のレコードを持つことができます。
m_parts テーブルの 1 件のレコードは、m_processes テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、production_m_process_orders テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、design_m_process_orders テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、maintenance_m_process_orders テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、modification_m_process_orders テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、drawing_m_process_orders テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、m_process_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、m_racks テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_parts テーブルの 1 件のレコードは、kakogawa_rack_part_stock_summaries テーブルの 1 件のレコードを持ちます。

==== MPartAttribute.php ====
m_part_attributes テーブルの 1 件のレコードは、m_parts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MPartCategory.php ====
m_part_categories テーブルの 1 件のレコードは、parent_categories テーブルの特定の 1 件のレコードに属します（外部キーは m_part_categories 側にあります）。
m_part_categories テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_part_categories 側にあります）。
m_part_categories テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_part_categories 側にあります）。
m_part_categories テーブルの 1 件のレコードは、m_parts テーブルの複数のレコードを持つことができます。
m_part_categories テーブルの 1 件のレコードは、child_categories テーブルの複数のレコードを持つことができます。

==== MPartFileType.php ====
m_part_file_types テーブルの 1 件のレコードは、t_part_files テーブルの複数のレコードを持つことができます。

==== MPaymentMethod.php ====
m_payment_methods テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードを持つことができます。
m_payment_methods テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードを持つことができます。

==== MPaymentSite.php ====
m_payment_sites テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードを持つことができます。
m_payment_sites テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードを持つことができます。

==== MPickingStatus.php ====
m_picking_statuses テーブルの 1 件のレコードは、t_production_picking_parts テーブルの複数のレコードを持つことができます。
m_picking_statuses テーブルの 1 件のレコードは、t_shipping_picking_parts テーブルの複数のレコードを持つことができます。

==== MPickingUsageType.php ====
m_picking_usage_types テーブルの 1 件のレコードは、t_pickings テーブルの複数のレコードを持つことができます。

==== MPrefecture.php ====
m_prefectures テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードを持つことができます。
m_prefectures テーブルの 1 件のレコードは、m_delivery_destinations テーブルの複数のレコードを持つことができます。
m_prefectures テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードを持つことができます。

==== MPrinter.php ====
m_printers テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードを持つことができます。

==== MPrivilegeType.php ====
m_privilege_types テーブルの 1 件のレコードは、m_employee_features テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_privilege_types テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_privilege_types テーブルの 1 件のレコードは、m_features テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MProcess.php ====
m_processes テーブルの 1 件のレコードは、m_process_types テーブルの特定の 1 件のレコードに属します（外部キーは m_processes 側にあります）。
m_processes テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_processes 側にあります）。
m_processes テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_processes 側にあります）。
m_processes テーブルの 1 件のレコードは、m_check_sheet_bases テーブルの複数のレコードを持つことができます。
m_processes テーブルの 1 件のレコードは、m_check_sheet_bases テーブルの 1 件のレコードを持ちます。
m_processes テーブルの 1 件のレコードは、m_parts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_processes テーブルの 1 件のレコードは、m_process_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_processes テーブルの 1 件のレコードは、pattern_m_process_patterns テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_processes テーブルの 1 件のレコードは、process_m_process_patterns テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_processes テーブルの 1 件のレコードは、t_design_process_orders テーブルの複数のレコードを持つことができます。
m_processes テーブルの 1 件のレコードは、t_production_process_orders テーブルの複数のレコードを持つことができます。
m_processes テーブルの 1 件のレコードは、m_groups テーブルの特定の 1 件のレコードに属します（外部キーは m_processes 側にあります）。

==== MProcessPattern.php ====
m_process_patterns テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_process_patterns 側にあります）。
m_process_patterns テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_process_patterns 側にあります）。
m_process_patterns テーブルの 1 件のレコードは、pattern_m_processes テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_process_patterns テーブルの 1 件のレコードは、m_processes テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_process_patterns テーブルの 1 件のレコードは、m_process_types テーブルの特定の 1 件のレコードに属します（外部キーは m_process_patterns 側にあります）。

==== MProcessType.php ====
m_process_types テーブルの 1 件のレコードは、m_parts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_process_types テーブルの 1 件のレコードは、m_processes テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_process_types テーブルの 1 件のレコードは、t_design_process_groups テーブルの複数のレコードを持つことができます。
m_process_types テーブルの 1 件のレコードは、t_production_process_groups テーブルの複数のレコードを持つことができます。
m_process_types テーブルの 1 件のレコードは、m_process_patterns テーブルの複数のレコードを持つことができます。

==== MProductionNumber.php ====
m_production_numbers テーブルの 1 件のレコードは、t_in_house_productions テーブルの特定の 1 件のレコードに属します（外部キーは m_production_numbers 側にあります）。
m_production_numbers テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは m_production_numbers 側にあります）。
m_production_numbers テーブルの 1 件のレコードは、m_machine_statuses テーブルの特定の 1 件のレコードに属します（外部キーは m_production_numbers 側にあります）。
m_production_numbers テーブルの 1 件のレコードは、m_machine_maintenance_statuses テーブルの特定の 1 件のレコードに属します（外部キーは m_production_numbers 側にあります）。
m_production_numbers テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは m_production_numbers 側にあります）。
m_production_numbers テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは m_production_numbers 側にあります）。
m_production_numbers テーブルの 1 件のレコードは、rotable_spare_part_m_production_nnumbers テーブルの特定の 1 件のレコードに属します（外部キーは m_production_numbers 側にあります）。
m_production_numbers テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_production_numbers 側にあります）。
m_production_numbers テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_production_numbers 側にあります）。
m_production_numbers テーブルの 1 件のレコードは、t_machine_returns テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_production_numbers テーブルの 1 件のレコードは、t_drawing_approval_requests テーブルの複数のレコードを持つことができます。
m_production_numbers テーブルの 1 件のレコードは、t_order_details テーブルの複数のレコードを持つことができます。
m_production_numbers テーブルの 1 件のレコードは、t_production_number_histories テーブルの複数のレコードを持つことができます。
m_production_numbers テーブルの 1 件のレコードは、t_pickings テーブルの複数のレコードを持つことができます。
m_production_numbers テーブルの 1 件のレコードは、t_production_picking_parts テーブルの複数のレコードを持つことができます。
m_production_numbers テーブルの 1 件のレコードは、t_shipping_details テーブルの複数のレコードを持つことができます。

==== MProductionProcessOrderStatus.php ====
m_production_process_order_statuses テーブルの 1 件のレコードは、t_production_process_orders テーブルの複数のレコードを持つことができます。

==== MPurchaseMethod.php ====
m_purchase_methods テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードを持つことができます。

==== MPurchaseOrderStatus.php ====
m_purchase_order_statuses テーブルの 1 件のレコードは、t_purchase_order_details テーブルの複数のレコードを持つことができます。
m_purchase_order_statuses テーブルの 1 件のレコードは、t_purchase_orders テーブルの複数のレコードを持つことができます。

==== MQuantityType.php ====

==== MRack.php ====
m_racks テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは m_racks 側にあります）。
m_racks テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_racks 側にあります）。
m_racks テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_racks 側にあります）。
m_racks テーブルの 1 件のレコードは、m_production_numbers テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、m_smartmats テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、t_inventories テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、t_production_picking_part_records テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、t_rack_part_stock_histories テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、t_rack_part_stocks テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、m_parts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_racks テーブルの 1 件のレコードは、t_shipping_picking_part_records テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、w_arrivals テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、w_production_pickings テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、w_shipping_pickings テーブルの複数のレコードを持つことができます。
m_racks テーブルの 1 件のレコードは、part_racks テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MRegistrationMethod.php ====
m_registration_methods テーブルの 1 件のレコードは、t_in_house_productions テーブルの複数のレコードを持つことができます。

==== MRemoteIsland.php ====
m_remote_islands テーブルの 1 件のレコードは、m_prefectures テーブルの特定の 1 件のレコードに属します（外部キーは m_remote_islands 側にあります）。
m_remote_islands テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_remote_islands 側にあります）。
m_remote_islands テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_remote_islands 側にあります）。

==== MReplenishmentType.php ====
m_replenishment_types テーブルの 1 件のレコードは、t_location_part_replenishment_points テーブルの複数のレコードを持つことができます。

==== MRoundType.php ====
m_round_types テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードを持つことができます。
m_round_types テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードを持つことができます。

==== MSalesDivision.php ====
m_sales_divisions テーブルの 1 件のレコードは、t_contracts テーブルの複数のレコードを持つことができます。
m_sales_divisions テーブルの 1 件のレコードは、t_order_details テーブルの複数のレコードを持つことができます。
m_sales_divisions テーブルの 1 件のレコードは、t_order_other_items テーブルの複数のレコードを持つことができます。
m_sales_divisions テーブルの 1 件のレコードは、t_orders テーブルの複数のレコードを持つことができます。
m_sales_divisions テーブルの 1 件のレコードは、t_shipping_other_items テーブルの複数のレコードを持つことができます。
不明なリレーション種別です。

==== MScheduleType.php ====
m_schedule_types テーブルの 1 件のレコードは、t_employee_schedules テーブルの複数のレコードを持つことができます。

==== MShip.php ====
m_ships テーブルの 1 件のレコードは、m_customers テーブルの特定の 1 件のレコードに属します（外部キーは m_ships 側にあります）。
m_ships テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_ships 側にあります）。
m_ships テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_ships 側にあります）。
m_ships テーブルの 1 件のレコードは、t_contracts テーブルの複数のレコードを持つことができます。

==== MShippingStatus.php ====
m_shipping_statuses テーブルの 1 件のレコードは、t_order_shippings テーブルの複数のレコードを持つことができます。
m_shipping_statuses テーブルの 1 件のレコードは、t_shippings テーブルの複数のレコードを持つことができます。

==== MShippingUsage.php ====
m_shipping_usages テーブルの 1 件のレコードは、t_shippings テーブルの複数のレコードを持つことができます。

==== MSmartmat.php ====
m_smartmats テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは m_smartmats 側にあります）。
m_smartmats テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは m_smartmats 側にあります）。
m_smartmats テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは m_smartmats 側にあります）。
m_smartmats テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_smartmats 側にあります）。
m_smartmats テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_smartmats 側にあります）。

==== MSmartmatSetting.php ====
m_smartmat_settings テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_smartmat_settings 側にあります）。
m_smartmat_settings テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_smartmat_settings 側にあります）。

==== MStockChangeReason.php ====
m_stock_change_reasons テーブルの 1 件のレコードは、t_rack_part_stock_histories テーブルの複数のレコードを持つことができます。

==== MStockHistoryType.php ====
m_stock_history_types テーブルの 1 件のレコードは、t_rack_part_stock_histories テーブルの複数のレコードを持つことができます。

==== MSupplier.php ====
m_suppliers テーブルの 1 件のレコードは、m_honorifics テーブルの特定の 1 件のレコードに属します（外部キーは m_suppliers 側にあります）。
m_suppliers テーブルの 1 件のレコードは、m_supplier_types テーブルの特定の 1 件のレコードに属します（外部キーは m_suppliers 側にあります）。
m_suppliers テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_suppliers 側にあります）。
m_suppliers テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_suppliers 側にあります）。
m_suppliers テーブルの 1 件のレコードは、m_purchase_methods テーブルの特定の 1 件のレコードに属します（外部キーは m_suppliers 側にあります）。
m_suppliers テーブルの 1 件のレコードは、m_prefectures テーブルの特定の 1 件のレコードに属します（外部キーは m_suppliers 側にあります）。
m_suppliers テーブルの 1 件のレコードは、m_round_types テーブルの特定の 1 件のレコードに属します（外部キーは m_suppliers 側にあります）。
m_suppliers テーブルの 1 件のレコードは、m_payment_methods テーブルの特定の 1 件のレコードに属します（外部キーは m_suppliers 側にあります）。
m_suppliers テーブルの 1 件のレコードは、m_parts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_suppliers テーブルの 1 件のレコードは、t_arrivals テーブルの複数のレコードを持つことができます。
m_suppliers テーブルの 1 件のレコードは、t_estimates テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_suppliers テーブルの 1 件のレコードは、t_estimate_suppliers テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_suppliers テーブルの 1 件のレコードは、t_purchase_orders テーブルの複数のレコードを持つことができます。
m_suppliers テーブルの 1 件のレコードは、m_external_accounts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_suppliers テーブルの 1 件のレコードは、m_supplier_banks テーブルの複数のレコードを持つことができます。

==== MSupplierBank.php ====
m_supplier_banks テーブルの 1 件のレコードは、m_suppliers テーブルの特定の 1 件のレコードに属します（外部キーは m_supplier_banks 側にあります）。
m_supplier_banks テーブルの 1 件のレコードは、m_bank_deposit_types テーブルの特定の 1 件のレコードに属します（外部キーは m_supplier_banks 側にあります）。
m_supplier_banks テーブルの 1 件のレコードは、m_transfer_fee_divisions テーブルの特定の 1 件のレコードに属します（外部キーは m_supplier_banks 側にあります）。
m_supplier_banks テーブルの 1 件のレコードは、m_payment_sites テーブルの特定の 1 件のレコードに属します（外部キーは m_supplier_banks 側にあります）。
m_supplier_banks テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_supplier_banks 側にあります）。
m_supplier_banks テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_supplier_banks 側にあります）。

==== MSupplierType.php ====
m_supplier_types テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードを持つことができます。

==== MTaxRate.php ====
m_tax_rates テーブルの 1 件のレコードは、t_purchase_order_details テーブルの複数のレコードを持つことができます。
m_tax_rates テーブルの 1 件のレコードは、t_purchase_orders テーブルの複数のレコードを持つことができます。

==== MTaxType.php ====
m_tax_types テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードを持つことができます。
m_tax_types テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードを持つことができます。
m_tax_types テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードを持つことができます。
m_tax_types テーブルの 1 件のレコードは、t_billings テーブルの複数のレコードを持つことができます。

==== MToMailGroup.php ====

==== MTransferFeeDivision.php ====
m_transfer_fee_divisions テーブルの 1 件のレコードは、m_suppliers テーブルの複数のレコードを持つことができます。
m_transfer_fee_divisions テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードを持つことができます。

==== MTransportCompany.php ====
m_transport_companies テーブルの 1 件のレコードは、m_honorifics テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_companies 側にあります）。
m_transport_companies テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_companies 側にあります）。
m_transport_companies テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_companies 側にあります）。
m_transport_companies テーブルの 1 件のレコードは、m_prefectures テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_companies 側にあります）。
m_transport_companies テーブルの 1 件のレコードは、m_round_types テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_companies 側にあります）。
m_transport_companies テーブルの 1 件のレコードは、m_payment_methods テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_companies 側にあります）。
m_transport_companies テーブルの 1 件のレコードは、group_m_car_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_transport_companies テーブルの 1 件のレコードは、t_charter_truck_request_groups テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_transport_companies テーブルの 1 件のレコードは、m_car_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_transport_companies テーブルの 1 件のレコードは、t_charter_truck_requests テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_transport_companies テーブルの 1 件のレコードは、m_customers テーブルの複数のレコードを持つことができます。
m_transport_companies テーブルの 1 件のレコードは、m_delivery_destinations テーブルの複数のレコードを持つことができます。
m_transport_companies テーブルの 1 件のレコードは、m_delivery_specific_times テーブルの複数のレコードを持つことができます。
m_transport_companies テーブルの 1 件のレコードは、t_charter_truck_request_arrangements テーブルの複数のレコードを持つことができます。
m_transport_companies テーブルの 1 件のレコードは、t_charter_truck_request_transport_companies テーブルの複数のレコードを持つことができます。
m_transport_companies テーブルの 1 件のレコードは、t_order_shippings テーブルの複数のレコードを持つことができます。
m_transport_companies テーブルの 1 件のレコードは、t_shippings テーブルの複数のレコードを持つことができます。
m_transport_companies テーブルの 1 件のレコードは、m_transport_company_banks テーブルの複数のレコードを持つことができます。
m_transport_companies テーブルの 1 件のレコードは、m_external_accounts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
m_transport_companies テーブルの 1 件のレコードは、m_transport_method_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MTransportCompanyBank.php ====
m_transport_company_banks テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_company_banks 側にあります）。
m_transport_company_banks テーブルの 1 件のレコードは、m_bank_deposit_types テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_company_banks 側にあります）。
m_transport_company_banks テーブルの 1 件のレコードは、m_transfer_fee_divisions テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_company_banks 側にあります）。
m_transport_company_banks テーブルの 1 件のレコードは、m_payment_sites テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_company_banks 側にあります）。
m_transport_company_banks テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_company_banks 側にあります）。
m_transport_company_banks テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは m_transport_company_banks 側にあります）。

==== MTransportMethodType.php ====
m_transport_method_types テーブルの 1 件のレコードは、t_order_shippings テーブルの複数のレコードを持つことができます。
m_transport_method_types テーブルの 1 件のレコードは、t_machine_returns テーブルの複数のレコードを持つことができます。
m_transport_method_types テーブルの 1 件のレコードは、t_shippings テーブルの複数のレコードを持つことができます。
m_transport_method_types テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== MUnitType.php ====
m_unit_types テーブルの 1 件のレコードは、m_parts テーブルの複数のレコードを持つことができます。
m_unit_types テーブルの 1 件のレコードは、t_order_other_items テーブルの複数のレコードを持つことができます。
m_unit_types テーブルの 1 件のレコードは、t_shipping_other_items テーブルの複数のレコードを持つことができます。

==== MVisitCycle.php ====
m_visit_cycles テーブルの 1 件のレコードは、t_maintenances テーブルの複数のレコードを持つことができます。

==== MWorkType.php ====
m_work_types テーブルの 1 件のレコードは、m_pages テーブルの複数のレコードを持つことができます。

==== PartProcessOrder.php ====
part_process_orders テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは part_process_orders 側にあります）。
part_process_orders テーブルの 1 件のレコードは、m_processes テーブルの特定の 1 件のレコードに属します（外部キーは part_process_orders 側にあります）。
part_process_orders テーブルの 1 件のレコードは、m_process_types テーブルの特定の 1 件のレコードに属します（外部キーは part_process_orders 側にあります）。

==== Session.php ====

==== TArrival.php ====
t_arrivals テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。
t_arrivals テーブルの 1 件のレコードは、m_suppliers テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。
t_arrivals テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。
t_arrivals テーブルの 1 件のレコードは、m_arrival_types テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。
t_arrivals テーブルの 1 件のレコードは、m_arrival_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。
t_arrivals テーブルの 1 件のレコードは、m_delivery_destinations テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。
t_arrivals テーブルの 1 件のレコードは、allocatiosns テーブルの複数のレコードを持つことができます。
t_arrivals テーブルの 1 件のレコードは、t_charter_truck_requests テーブルの複数のレコードを持つことができます。
t_arrivals テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。
t_arrivals テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。
t_arrivals テーブルの 1 件のレコードは、m_production_numbers テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。
t_arrivals テーブルの 1 件のレコードは、m_customers テーブルの特定の 1 件のレコードに属します（外部キーは t_arrivals 側にあります）。

==== TBilling.php ====
不明なリレーション種別です。
t_billings テーブルの 1 件のレコードは、t_orders テーブルの特定の 1 件のレコードに属します（外部キーは t_billings 側にあります）。
t_billings テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_billings 側にあります）。
t_billings テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_billings 側にあります）。
t_billings テーブルの 1 件のレコードは、t_billing_details テーブルの複数のレコードを持つことができます。
t_billings テーブルの 1 件のレコードは、m_contract_update_divisions テーブルの特定の 1 件のレコードに属します（外部キーは t_billings 側にあります）。

==== TBillingDetail.php ====
t_billing_details テーブルの 1 件のレコードは、t_billings テーブルの特定の 1 件のレコードに属します（外部キーは t_billing_details 側にあります）。
t_billing_details テーブルの 1 件のレコードは、m_billing_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_billing_details 側にあります）。
t_billing_details テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_billing_details 側にあります）。
t_billing_details テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_billing_details 側にあります）。

==== TCharterTruckEstimate.php ====
t_charter_truck_estimates テーブルの 1 件のレコードは、m_car_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_estimates 側にあります）。
t_charter_truck_estimates テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_estimates 側にあります）。
t_charter_truck_estimates テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_estimates 側にあります）。
t_charter_truck_estimates テーブルの 1 件のレコードは、t_charter_truck_estimate_details テーブルの複数のレコードを持つことができます。
t_charter_truck_estimates テーブルの 1 件のレコードは、t_charter_truck_request_transport_companies テーブルの複数のレコードを持つことができます。

==== TCharterTruckEstimateDetail.php ====
t_charter_truck_estimate_details テーブルの 1 件のレコードは、t_charter_truck_estimates テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_estimate_details 側にあります）。
t_charter_truck_estimate_details テーブルの 1 件のレコードは、t_charter_truck_requests テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_estimate_details 側にあります）。
t_charter_truck_estimate_details テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_estimate_details 側にあります）。
t_charter_truck_estimate_details テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_estimate_details 側にあります）。

==== TCharterTruckRequest.php ====
不明なリレーション種別です。
t_charter_truck_requests テーブルの 1 件のレコードは、t_shippings テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_requests 側にあります）。
t_charter_truck_requests テーブルの 1 件のレコードは、t_arrivals テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_requests 側にあります）。
t_charter_truck_requests テーブルの 1 件のレコードは、t_charter_truck_request_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_requests 側にあります）。
t_charter_truck_requests テーブルの 1 件のレコードは、m_prefectures テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_requests 側にあります）。
t_charter_truck_requests テーブルの 1 件のレコードは、m_car_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_requests 側にあります）。
t_charter_truck_requests テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_requests 側にあります）。
t_charter_truck_requests テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_requests 側にあります）。
t_charter_truck_requests テーブルの 1 件のレコードは、m_car_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_charter_truck_requests テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_charter_truck_requests テーブルの 1 件のレコードは、t_charter_truck_estimate_details テーブルの複数のレコードを持つことができます。

==== TCharterTruckRequestArrangement.php ====
t_charter_truck_request_arrangements テーブルの 1 件のレコードは、t_charter_truck_request_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_arrangements 側にあります）。
t_charter_truck_request_arrangements テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_arrangements 側にあります）。
t_charter_truck_request_arrangements テーブルの 1 件のレコードは、m_charter_truck_request_arrangement_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_arrangements 側にあります）。
t_charter_truck_request_arrangements テーブルの 1 件のレコードは、m_car_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_arrangements 側にあります）。
t_charter_truck_request_arrangements テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_arrangements 側にあります）。
t_charter_truck_request_arrangements テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_arrangements 側にあります）。
t_charter_truck_request_arrangements テーブルの 1 件のレコードは、t_charter_truck_request_arrangement_details テーブルの複数のレコードを持つことができます。

==== TCharterTruckRequestArrangementDetail.php ====
t_charter_truck_request_arrangement_details テーブルの 1 件のレコードは、t_charter_truck_request_arrangements テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_arrangement_details 側にあります）。
t_charter_truck_request_arrangement_details テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_arrangement_details 側にあります）。
t_charter_truck_request_arrangement_details テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_arrangement_details 側にあります）。

==== TCharterTruckRequestGroup.php ====
t_charter_truck_request_groups テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_groups 側にあります）。
t_charter_truck_request_groups テーブルの 1 件のレコードは、m_car_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_groups 側にあります）。
t_charter_truck_request_groups テーブルの 1 件のレコードは、m_charter_truck_request_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_groups 側にあります）。
t_charter_truck_request_groups テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_groups 側にあります）。
t_charter_truck_request_groups テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_groups 側にあります）。
t_charter_truck_request_groups テーブルの 1 件のレコードは、m_car_types テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_charter_truck_request_groups テーブルの 1 件のレコードは、m_transport_companies テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_charter_truck_request_groups テーブルの 1 件のレコードは、t_charter_truck_request_arrangements テーブルの複数のレコードを持つことができます。
t_charter_truck_request_groups テーブルの 1 件のレコードは、t_charter_truck_request_transport_companies テーブルの複数のレコードを持つことができます。
t_charter_truck_request_groups テーブルの 1 件のレコードは、t_charter_truck_requests テーブルの複数のレコードを持つことができます。

==== TCharterTruckRequestTransportCompany.php ====
t_charter_truck_request_transport_companies テーブルの 1 件のレコードは、t_charter_truck_request_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_transport_companies 側にあります）。
t_charter_truck_request_transport_companies テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_transport_companies 側にあります）。
t_charter_truck_request_transport_companies テーブルの 1 件のレコードは、m_charter_truck_request_entry_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_transport_companies 側にあります）。
t_charter_truck_request_transport_companies テーブルの 1 件のレコードは、t_charter_truck_estimates テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_transport_companies 側にあります）。
t_charter_truck_request_transport_companies テーブルの 1 件のレコードは、m_charter_truck_request_order_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_transport_companies 側にあります）。
t_charter_truck_request_transport_companies テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_transport_companies 側にあります）。
t_charter_truck_request_transport_companies テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_charter_truck_request_transport_companies 側にあります）。

==== TCheckSheetApprovalRequest.php ====
t_check_sheet_approval_requests テーブルの 1 件のレコードは、t_check_sheet_records テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_approval_requests 側にあります）。
t_check_sheet_approval_requests テーブルの 1 件のレコードは、m_check_sheet_approval_request_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_approval_requests 側にあります）。
t_check_sheet_approval_requests テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_approval_requests 側にあります）。
t_check_sheet_approval_requests テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_approval_requests 側にあります）。
t_check_sheet_approval_requests テーブルの 1 件のレコードは、t_check_sheet_approval_request_histories テーブルの複数のレコードを持つことができます。

==== TCheckSheetApprovalRequestHistory.php ====
t_check_sheet_approval_request_histories テーブルの 1 件のレコードは、t_check_sheet_approval_requests テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_approval_request_histories 側にあります）。
t_check_sheet_approval_request_histories テーブルの 1 件のレコードは、m_check_sheet_approval_request_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_approval_request_histories 側にあります）。
t_check_sheet_approval_request_histories テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_approval_request_histories 側にあります）。
t_check_sheet_approval_request_histories テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_approval_request_histories 側にあります）。
t_check_sheet_approval_request_histories テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_approval_request_histories 側にあります）。

==== TCheckSheetRecord.php ====
t_check_sheet_records テーブルの 1 件のレコードは、t_production_process_order_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_records 側にあります）。
t_check_sheet_records テーブルの 1 件のレコードは、m_check_sheet_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_records 側にあります）。
t_check_sheet_records テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_records 側にあります）。
t_check_sheet_records テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_records 側にあります）。
t_check_sheet_records テーブルの 1 件のレコードは、t_check_sheet_approval_requests テーブルの複数のレコードを持つことができます。
t_check_sheet_records テーブルの 1 件のレコードは、t_check_sheet_record_details テーブルの複数のレコードを持つことができます。

==== TCheckSheetRecordDetail.php ====
t_check_sheet_record_details テーブルの 1 件のレコードは、t_check_sheet_records テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_record_details 側にあります）。
t_check_sheet_record_details テーブルの 1 件のレコードは、m_check_sheet_details テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_record_details 側にあります）。
t_check_sheet_record_details テーブルの 1 件のレコードは、m_check_sheet_detail_types テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_record_details 側にあります）。
t_check_sheet_record_details テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_record_details 側にあります）。
t_check_sheet_record_details テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_check_sheet_record_details 側にあります）。

==== TConfigurationPartDetail.php ====
t_configuration_part_details テーブルの 1 件のレコードは、t_in_house_productions テーブルの特定の 1 件のレコードに属します（外部キーは t_configuration_part_details 側にあります）。
t_configuration_part_details テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_configuration_part_details 側にあります）。
t_configuration_part_details テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_configuration_part_details 側にあります）。
t_configuration_part_details テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_configuration_part_details 側にあります）。
t_configuration_part_details テーブルの 1 件のレコードは、allocations テーブルの 1 件のレコードを持ちます。
t_configuration_part_details テーブルの 1 件のレコードは、t_in_house_production_relations テーブルの複数のレコードを持つことができます。

==== TContract.php ====
t_contracts テーブルの 1 件のレコードは、ship_owner_m_customers テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、engine_maker_m_customers テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、end_user_m_customers テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、noc_m_customers テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、m_liquid_types テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、previous_t_contracts テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、machine_t_contracts テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、m_sales_divisions テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、m_ships テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、m_factory_categories テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、m_equipment テーブルの特定の 1 件のレコードに属します（外部キーは t_contracts 側にあります）。
t_contracts テーブルの 1 件のレコードは、t_contract_files テーブルの複数のレコードを持つことができます。
t_contracts テーブルの 1 件のレコードは、t_drawing_approval_requests テーブルの複数のレコードを持つことができます。
t_contracts テーブルの 1 件のレコードは、t_machine_returns テーブルの複数のレコードを持つことができます。
t_contracts テーブルの 1 件のレコードは、t_maintenances テーブルの 1 件のレコードを持ちます。
t_contracts テーブルの 1 件のレコードは、t_maintenance_works テーブルの複数のレコードを持つことができます。
t_contracts テーブルの 1 件のレコードは、t_orders テーブルの複数のレコードを持つことができます。
t_contracts テーブルの 1 件のレコードは、first_orders テーブルの 1 件のレコードを持ちます。

==== TContractFile.php ====
t_contract_files テーブルの 1 件のレコードは、t_contracts テーブルの特定の 1 件のレコードに属します（外部キーは t_contract_files 側にあります）。
t_contract_files テーブルの 1 件のレコードは、t_orders テーブルの特定の 1 件のレコードに属します（外部キーは t_contract_files 側にあります）。
t_contract_files テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_contract_files 側にあります）。
t_contract_files テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_contract_files 側にあります）。

==== TDesignProcessGroup.php ====
不明なリレーション種別です。
t_design_process_groups テーブルの 1 件のレコードは、m_process_types テーブルの特定の 1 件のレコードに属します（外部キーは t_design_process_groups 側にあります）。
t_design_process_groups テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_design_process_groups 側にあります）。
t_design_process_groups テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_design_process_groups 側にあります）。
t_design_process_groups テーブルの 1 件のレコードは、design_t_in_house_productions テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_design_process_groups テーブルの 1 件のレコードは、t_design_process_group_drawing_files テーブルの複数のレコードを持つことができます。
t_design_process_groups テーブルの 1 件のレコードは、t_design_process_orders テーブルの複数のレコードを持つことができます。
t_design_process_groups テーブルの 1 件のレコードは、t_drawing_approval_requests テーブルの複数のレコードを持つことができます。
不明なリレーション種別です。

==== TDesignProcessGroupDrawingFile.php ====
t_design_process_group_drawing_files テーブルの 1 件のレコードは、t_design_process_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_design_process_group_drawing_files 側にあります）。

==== TDesignProcessOrder.php ====
不明なリレーション種別です。
t_design_process_orders テーブルの 1 件のレコードは、t_design_process_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_design_process_orders 側にあります）。
t_design_process_orders テーブルの 1 件のレコードは、m_processes テーブルの特定の 1 件のレコードに属します（外部キーは t_design_process_orders 側にあります）。
t_design_process_orders テーブルの 1 件のレコードは、m_design_process_order_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_design_process_orders 側にあります）。
t_design_process_orders テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_design_process_orders 側にあります）。
t_design_process_orders テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_design_process_orders 側にあります）。
t_design_process_orders テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
不明なリレーション種別です。

==== TDrawingApprovalRequest.php ====
不明なリレーション種別です。
t_drawing_approval_requests テーブルの 1 件のレコードは、t_design_process_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_requests 側にあります）。
t_drawing_approval_requests テーブルの 1 件のレコードは、m_production_numbers テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_requests 側にあります）。
t_drawing_approval_requests テーブルの 1 件のレコードは、m_drawing_approval_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_requests 側にあります）。
t_drawing_approval_requests テーブルの 1 件のレコードは、t_contracts テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_requests 側にあります）。
t_drawing_approval_requests テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_requests 側にあります）。
t_drawing_approval_requests テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_requests 側にあります）。
t_drawing_approval_requests テーブルの 1 件のレコードは、t_drawing_approval_request_histories テーブルの複数のレコードを持つことができます。
t_drawing_approval_requests テーブルの 1 件のレコードは、created_bies テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_requests 側にあります）。

==== TDrawingApprovalRequestHistory.php ====
t_drawing_approval_request_histories テーブルの 1 件のレコードは、t_drawing_approval_requests テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_request_histories 側にあります）。
t_drawing_approval_request_histories テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_request_histories 側にあります）。
t_drawing_approval_request_histories テーブルの 1 件のレコードは、m_drawing_approval_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_request_histories 側にあります）。
t_drawing_approval_request_histories テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_request_histories 側にあります）。
t_drawing_approval_request_histories テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_drawing_approval_request_histories 側にあります）。

==== TEmployeeSchedule.php ====
t_employee_schedules テーブルの 1 件のレコードは、m_schedule_types テーブルの特定の 1 件のレコードに属します（外部キーは t_employee_schedules 側にあります）。
t_employee_schedules テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_employee_schedules 側にあります）。
t_employee_schedules テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_employee_schedules 側にあります）。
t_employee_schedules テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== TEstimate.php ====
t_estimates テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_estimates 側にあります）。
t_estimates テーブルの 1 件のレコードは、m_delivery_destinations テーブルの特定の 1 件のレコードに属します（外部キーは t_estimates 側にあります）。
t_estimates テーブルの 1 件のレコードは、m_delivery_methods テーブルの特定の 1 件のレコードに属します（外部キーは t_estimates 側にあります）。
t_estimates テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_estimates 側にあります）。
t_estimates テーブルの 1 件のレコードは、m_estimate_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_estimates 側にあります）。
t_estimates テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_estimates 側にあります）。
t_estimates テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_estimates 側にあります）。
t_estimates テーブルの 1 件のレコードは、allocations テーブルの 1 件のレコードを持ちます。
t_estimates テーブルの 1 件のレコードは、t_estimate_deliveries テーブルの複数のレコードを持つことができます。
t_estimates テーブルの 1 件のレコードは、t_purchase_orders テーブルの複数のレコードを持つことができます。
t_estimates テーブルの 1 件のレコードは、t_estimate_suppliers テーブルの複数のレコードを持つことができます。

==== TEstimateDelivery.php ====
t_estimate_deliveries テーブルの 1 件のレコードは、t_estimates テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_deliveries 側にあります）。
t_estimate_deliveries テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_deliveries 側にあります）。
t_estimate_deliveries テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_deliveries 側にあります）。
t_estimate_deliveries テーブルの 1 件のレコードは、t_estimate_supplier_deliveries テーブルの複数のレコードを持つことができます。

==== TEstimateSupplier.php ====
t_estimate_suppliers テーブルの 1 件のレコードは、t_estimates テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_suppliers 側にあります）。
t_estimate_suppliers テーブルの 1 件のレコードは、m_suppliers テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_suppliers 側にあります）。
t_estimate_suppliers テーブルの 1 件のレコードは、t_estimate_supplier_deliveries テーブルの複数のレコードを持つことができます。
t_estimate_suppliers テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_suppliers 側にあります）。
t_estimate_suppliers テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_suppliers 側にあります）。

==== TEstimateSupplierDelivery.php ====
t_estimate_supplier_deliveries テーブルの 1 件のレコードは、t_estimate_suppliers テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_supplier_deliveries 側にあります）。
t_estimate_supplier_deliveries テーブルの 1 件のレコードは、t_estimate_deliveries テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_supplier_deliveries 側にあります）。
t_estimate_supplier_deliveries テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_supplier_deliveries 側にあります）。
t_estimate_supplier_deliveries テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_estimate_supplier_deliveries 側にあります）。

==== TExternalAccountRecord.php ====
t_external_account_records テーブルの 1 件のレコードは、m_external_accounts テーブルの特定の 1 件のレコードに属します（外部キーは t_external_account_records 側にあります）。
t_external_account_records テーブルの 1 件のレコードは、m_external_account_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_external_account_records 側にあります）。
t_external_account_records テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_external_account_records 側にあります）。
t_external_account_records テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_external_account_records 側にあります）。
t_external_account_records テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_external_account_records 側にあります）。

==== TInHouseProduction.php ====
t_in_house_productions テーブルの 1 件のレコードは、m_registration_methods テーブルの特定の 1 件のレコードに属します（外部キーは t_in_house_productions 側にあります）。
t_in_house_productions テーブルの 1 件のレコードは、m_instruction_works テーブルの特定の 1 件のレコードに属します（外部キーは t_in_house_productions 側にあります）。
t_in_house_productions テーブルの 1 件のレコードは、m_in_house_production_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_in_house_productions 側にあります）。
t_in_house_productions テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_in_house_productions 側にあります）。
t_in_house_productions テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_in_house_productions 側にあります）。
t_in_house_productions テーブルの 1 件のレコードは、design_t_design_process_groups テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_in_house_productions テーブルの 1 件のレコードは、m_production_numbers テーブルの複数のレコードを持つことができます。
t_in_house_productions テーブルの 1 件のレコードは、production_t_production_process_groups テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_in_house_productions テーブルの 1 件のレコードは、t_configuration_part_details テーブルの複数のレコードを持つことができます。
t_in_house_productions テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_in_house_productions 側にあります）。
t_in_house_productions テーブルの 1 件のレコードは、parent_products テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_in_house_productions テーブルの 1 件のレコードは、child_products テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== TInHouseProductionRelation.php ====
t_in_house_production_relations テーブルの 1 件のレコードは、child_products テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_in_house_production_relations テーブルの 1 件のレコードは、parent_products テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_in_house_production_relations テーブルの 1 件のレコードは、parent_configuration_part_details テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== TInventory.php ====
t_inventories テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは t_inventories 側にあります）。
t_inventories テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_inventories 側にあります）。
t_inventories テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_inventories 側にあります）。
t_inventories テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_inventories 側にあります）。
t_inventories テーブルの 1 件のレコードは、m_smartmats テーブルの特定の 1 件のレコードに属します（外部キーは t_inventories 側にあります）。
t_inventories テーブルの 1 件のレコードは、m_stock_history_types テーブルの特定の 1 件のレコードに属します（外部キーは t_inventories 側にあります）。
t_inventories テーブルの 1 件のレコードは、m_stock_change_reasons テーブルの特定の 1 件のレコードに属します（外部キーは t_inventories 側にあります）。

==== TLocationPartReplenishmentPoint.php ====
t_location_part_replenishment_points テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_location_part_replenishment_points 側にあります）。
t_location_part_replenishment_points テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは t_location_part_replenishment_points 側にあります）。
t_location_part_replenishment_points テーブルの 1 件のレコードは、m_replenishment_types テーブルの特定の 1 件のレコードに属します（外部キーは t_location_part_replenishment_points 側にあります）。
t_location_part_replenishment_points テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_location_part_replenishment_points 側にあります）。
t_location_part_replenishment_points テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_location_part_replenishment_points 側にあります）。

==== TMachineReturn.php ====
t_machine_returns テーブルの 1 件のレコードは、t_contracts テーブルの特定の 1 件のレコードに属します（外部キーは t_machine_returns 側にあります）。
t_machine_returns テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは t_machine_returns 側にあります）。
t_machine_returns テーブルの 1 件のレコードは、m_transport_method_types テーブルの特定の 1 件のレコードに属します（外部キーは t_machine_returns 側にあります）。
t_machine_returns テーブルの 1 件のレコードは、m_car_types テーブルの特定の 1 件のレコードに属します（外部キーは t_machine_returns 側にあります）。
t_machine_returns テーブルの 1 件のレコードは、t_order_details テーブルの特定の 1 件のレコードに属します（外部キーは t_machine_returns 側にあります）。
t_machine_returns テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_machine_returns 側にあります）。
t_machine_returns テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_machine_returns 側にあります）。
t_machine_returns テーブルの 1 件のレコードは、m_production_numbers テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_machine_returns テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは t_machine_returns 側にあります）。

==== TMaintenance.php ====
不明なリレーション種別です。
t_maintenances テーブルの 1 件のレコードは、t_contracts テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenances 側にあります）。
t_maintenances テーブルの 1 件のレコードは、t_orders テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenances 側にあります）。
t_maintenances テーブルの 1 件のレコードは、m_visit_cycles テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenances 側にあります）。
t_maintenances テーブルの 1 件のレコードは、m_maintenance_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenances 側にあります）。
t_maintenances テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenances 側にあります）。
t_maintenances テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenances 側にあります）。
t_maintenances テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_maintenances テーブルの 1 件のレコードは、t_maintenance_works テーブルの複数のレコードを持つことができます。

==== TMaintenanceWork.php ====
不明なリレーション種別です。
t_maintenance_works テーブルの 1 件のレコードは、t_maintenances テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_works 側にあります）。
t_maintenance_works テーブルの 1 件のレコードは、t_contracts テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_works 側にあります）。
t_maintenance_works テーブルの 1 件のレコードは、m_maintenance_work_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_works 側にあります）。
t_maintenance_works テーブルの 1 件のレコードは、m_maintenance_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_works 側にあります）。
t_maintenance_works テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_works 側にあります）。
t_maintenance_works テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_works 側にあります）。
t_maintenance_works テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_maintenance_works テーブルの 1 件のレコードは、m_company_cars テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_maintenance_works テーブルの 1 件のレコードは、t_maintenance_work_records テーブルの複数のレコードを持つことができます。
t_maintenance_works テーブルの 1 件のレコードは、employee_maintenance_works テーブルの複数のレコードを持つことができます。

==== TMaintenanceWorkRecord.php ====
t_maintenance_work_records テーブルの 1 件のレコードは、t_maintenance_works テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_work_records 側にあります）。
t_maintenance_work_records テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_work_records 側にあります）。
t_maintenance_work_records テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_maintenance_work_records テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_work_records 側にあります）。
t_maintenance_work_records テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_work_records 側にあります）。
t_maintenance_work_records テーブルの 1 件のレコードは、t_maintenance_work_record_pause_times テーブルの複数のレコードを持つことができます。

==== TMaintenanceWorkRecordPauseTime.php ====
t_maintenance_work_record_pause_times テーブルの 1 件のレコードは、t_maintenance_work_records テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_work_record_pause_times 側にあります）。
t_maintenance_work_record_pause_times テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_work_record_pause_times 側にあります）。
t_maintenance_work_record_pause_times テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_maintenance_work_record_pause_times 側にあります）。

==== TNotice.php ====
t_notices テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_notices 側にあります）。
t_notices テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_notices 側にあります）。
t_notices テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_notices 側にあります）。
t_notices テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== TOrder.php ====
t_orders テーブルの 1 件のレコードは、t_contracts テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、m_order_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、m_sales_divisions テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、other_invoice_to_m_customers テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、m_customer_persons テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_orders テーブルの 1 件のレコードは、t_billings テーブルの複数のレコードを持つことができます。
t_orders テーブルの 1 件のレコードは、t_maintenances テーブルの 1 件のレコードを持ちます。
t_orders テーブルの 1 件のレコードは、t_order_details テーブルの複数のレコードを持つことができます。
t_orders テーブルの 1 件のレコードは、t_order_other_items テーブルの複数のレコードを持つことができます。
t_orders テーブルの 1 件のレコードは、m_invoice_send_methods テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、m_tax_types テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、m_tax_rates テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、invoice_to_m_customer_types テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、delivery_note_to_m_customer_types テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、t_order_shippings テーブルの複数のレコードを持つことができます。
t_orders テーブルの 1 件のレコードは、in_progress_t_order_shippings テーブルの 1 件のレコードを持ちます。
t_orders テーブルの 1 件のレコードは、main_order_details テーブルの 1 件のレコードを持ちます。
t_orders テーブルの 1 件のレコードは、t_shippings テーブルの複数のレコードを持つことができます。
t_orders テーブルの 1 件のレコードは、case_mark_files テーブルの 1 件のレコードを持ちます。
t_orders テーブルの 1 件のレコードは、invoice_and_delivery_note_to_m_customer_types テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、m_round_types テーブルの特定の 1 件のレコードに属します（外部キーは t_orders 側にあります）。
t_orders テーブルの 1 件のレコードは、t_maintenances テーブルの複数のレコードを持つことができます。

==== TOrderDetail.php ====
不明なリレーション種別です。
不明なリレーション種別です。
不明なリレーション種別です。
不明なリレーション種別です。
t_order_details テーブルの 1 件のレコードは、t_orders テーブルの特定の 1 件のレコードに属します（外部キーは t_order_details 側にあります）。
t_order_details テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_order_details 側にあります）。
t_order_details テーブルの 1 件のレコードは、m_production_numbers テーブルの特定の 1 件のレコードに属します（外部キーは t_order_details 側にあります）。
t_order_details テーブルの 1 件のレコードは、m_delivery_destinations テーブルの特定の 1 件のレコードに属します（外部キーは t_order_details 側にあります）。
t_order_details テーブルの 1 件のレコードは、m_sales_divisions テーブルの特定の 1 件のレコードに属します（外部キーは t_order_details 側にあります）。
t_order_details テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_order_details 側にあります）。
t_order_details テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_order_details 側にあります）。
t_order_details テーブルの 1 件のレコードは、allocations テーブルの 1 件のレコードを持ちます。
t_order_details テーブルの 1 件のレコードは、t_machine_returns テーブルの 1 件のレコードを持ちます。
t_order_details テーブルの 1 件のレコードは、t_shipping_details テーブルの複数のレコードを持つことができます。
t_order_details テーブルの 1 件のレコードは、t_shipping_details テーブルの 1 件のレコードを持ちます。

==== TOrderOtherItem.php ====
不明なリレーション種別です。
不明なリレーション種別です。
不明なリレーション種別です。
t_order_other_items テーブルの 1 件のレコードは、t_orders テーブルの特定の 1 件のレコードに属します（外部キーは t_order_other_items 側にあります）。
t_order_other_items テーブルの 1 件のレコードは、m_sales_divisions テーブルの特定の 1 件のレコードに属します（外部キーは t_order_other_items 側にあります）。
t_order_other_items テーブルの 1 件のレコードは、m_unit_types テーブルの特定の 1 件のレコードに属します（外部キーは t_order_other_items 側にあります）。
t_order_other_items テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_order_other_items 側にあります）。
t_order_other_items テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_order_other_items 側にあります）。
t_order_other_items テーブルの 1 件のレコードは、m_overhaul_types テーブルの特定の 1 件のレコードに属します（外部キーは t_order_other_items 側にあります）。

==== TOrderShipping.php ====
t_order_shippings テーブルの 1 件のレコードは、t_orders テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。
t_order_shippings テーブルの 1 件のレコードは、ship_to_m_delivery_destinations テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。
t_order_shippings テーブルの 1 件のレコードは、ship_from_m_delivery_destinations テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。
t_order_shippings テーブルの 1 件のレコードは、ship_to_m_delivery_destination_m_honorifics テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。
t_order_shippings テーブルの 1 件のレコードは、m_transport_method_types テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。
t_order_shippings テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。
t_order_shippings テーブルの 1 件のレコードは、m_delivery_specific_times テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。
t_order_shippings テーブルの 1 件のレコードは、m_shipping_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。
t_order_shippings テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。
t_order_shippings テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_order_shippings 側にあります）。

==== TPartFile.php ====
t_part_files テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_part_files 側にあります）。
t_part_files テーブルの 1 件のレコードは、m_part_file_types テーブルの特定の 1 件のレコードに属します（外部キーは t_part_files 側にあります）。
t_part_files テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_part_files 側にあります）。
t_part_files テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_part_files 側にあります）。

==== TPicking.php ====
t_pickings テーブルの 1 件のレコードは、t_shippings テーブルの特定の 1 件のレコードに属します（外部キーは t_pickings 側にあります）。
t_pickings テーブルの 1 件のレコードは、m_locations テーブルの特定の 1 件のレコードに属します（外部キーは t_pickings 側にあります）。
t_pickings テーブルの 1 件のレコードは、m_picking_usage_types テーブルの特定の 1 件のレコードに属します（外部キーは t_pickings 側にあります）。
t_pickings テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_pickings 側にあります）。
t_pickings テーブルの 1 件のレコードは、t_production_process_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_pickings 側にあります）。
t_pickings テーブルの 1 件のレコードは、m_production_numbers テーブルの特定の 1 件のレコードに属します（外部キーは t_pickings 側にあります）。
t_pickings テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_pickings 側にあります）。
t_pickings テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_pickings 側にあります）。
t_pickings テーブルの 1 件のレコードは、t_picking_boxes テーブルの複数のレコードを持つことができます。
t_pickings テーブルの 1 件のレコードは、t_production_picking_parts テーブルの複数のレコードを持つことができます。
t_pickings テーブルの 1 件のレコードは、t_shipping_picking_parts テーブルの複数のレコードを持つことができます。

==== TPickingBox.php ====
t_picking_boxes テーブルの 1 件のレコードは、t_pickings テーブルの特定の 1 件のレコードに属します（外部キーは t_picking_boxes 側にあります）。
t_picking_boxes テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_picking_boxes 側にあります）。
t_picking_boxes テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_picking_boxes 側にあります）。
t_picking_boxes テーブルの 1 件のレコードは、t_shipping_picking_parts テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。

==== TProductionNumberHistory.php ====
t_production_number_histories テーブルの 1 件のレコードは、m_production_numbers テーブルの特定の 1 件のレコードに属します（外部キーは t_production_number_histories 側にあります）。
t_production_number_histories テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_number_histories 側にあります）。
t_production_number_histories テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_number_histories 側にあります）。

==== TProductionPickingPart.php ====
t_production_picking_parts テーブルの 1 件のレコードは、t_production_process_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_parts 側にあります）。
t_production_picking_parts テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_parts 側にあります）。
t_production_picking_parts テーブルの 1 件のレコードは、m_production_numbers テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_parts 側にあります）。
t_production_picking_parts テーブルの 1 件のレコードは、m_picking_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_parts 側にあります）。
t_production_picking_parts テーブルの 1 件のレコードは、t_pickings テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_parts 側にあります）。
t_production_picking_parts テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_parts 側にあります）。
t_production_picking_parts テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_parts 側にあります）。
t_production_picking_parts テーブルの 1 件のレコードは、t_production_picking_part_records テーブルの複数のレコードを持つことができます。

==== TProductionPickingPartRecord.php ====
t_production_picking_part_records テーブルの 1 件のレコードは、t_production_picking_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_part_records 側にあります）。
t_production_picking_part_records テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_part_records 側にあります）。
t_production_picking_part_records テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_part_records 側にあります）。
t_production_picking_part_records テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_part_records 側にあります）。
t_production_picking_part_records テーブルの 1 件のレコードは、created_by_m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_production_picking_part_records 側にあります）。

==== TProductionProcessChange.php ====
t_production_process_changes テーブルの 1 件のレコードは、t_production_process_orders テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_changes 側にあります）。
t_production_process_changes テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_changes 側にあります）。
t_production_process_changes テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_changes 側にあります）。
t_production_process_changes テーブルの 1 件のレコードは、t_production_process_change_logs テーブルの複数のレコードを持つことができます。

==== TProductionProcessChangeLog.php ====
t_production_process_change_logs テーブルの 1 件のレコードは、t_production_process_changes テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_change_logs 側にあります）。
t_production_process_change_logs テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_change_logs 側にあります）。
t_production_process_change_logs テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_change_logs 側にあります）。
t_production_process_change_logs テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_change_logs 側にあります）。

==== TProductionProcessGroup.php ====
不明なリレーション種別です。
t_production_process_groups テーブルの 1 件のレコードは、m_process_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_groups 側にあります）。
t_production_process_groups テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_groups 側にあります）。
t_production_process_groups テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_groups 側にあります）。
t_production_process_groups テーブルの 1 件のレコードは、t_pickings テーブルの複数のレコードを持つことができます。
t_production_process_groups テーブルの 1 件のレコードは、t_production_picking_parts テーブルの複数のレコードを持つことができます。
t_production_process_groups テーブルの 1 件のレコードは、t_production_process_orders テーブルの複数のレコードを持つことができます。
t_production_process_groups テーブルの 1 件のレコードは、t_production_rejects テーブルの複数のレコードを持つことができます。
t_production_process_groups テーブルの 1 件のレコードは、w_production_pickings テーブルの複数のレコードを持つことができます。
t_production_process_groups テーブルの 1 件のレコードは、production_t_in_house_productions テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
不明なリレーション種別です。

==== TProductionProcessOrder.php ====
不明なリレーション種別です。
t_production_process_orders テーブルの 1 件のレコードは、t_production_process_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_orders 側にあります）。
t_production_process_orders テーブルの 1 件のレコードは、t_production_process_order_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_orders 側にあります）。
t_production_process_orders テーブルの 1 件のレコードは、m_processes テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_orders 側にあります）。
t_production_process_orders テーブルの 1 件のレコードは、approval_m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_orders 側にあります）。
t_production_process_orders テーブルの 1 件のレコードは、m_production_process_order_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_orders 側にあります）。
t_production_process_orders テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_orders 側にあります）。
t_production_process_orders テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_orders 側にあります）。
t_production_process_orders テーブルの 1 件のレコードは、m_employees テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_production_process_orders テーブルの 1 件のレコードは、t_production_process_changes テーブルの複数のレコードを持つことができます。
不明なリレーション種別です。

==== TProductionProcessOrderGroup.php ====
不明なリレーション種別です。
t_production_process_order_groups テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_order_groups 側にあります）。
t_production_process_order_groups テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_order_groups 側にあります）。
t_production_process_order_groups テーブルの 1 件のレコードは、t_check_sheet_records テーブルの 1 件のレコードを持ちます。
t_production_process_order_groups テーブルの 1 件のレコードは、t_production_process_order_group_records テーブルの 1 件のレコードを持ちます。
t_production_process_order_groups テーブルの 1 件のレコードは、t_production_process_order_group_record_pause_times テーブルの複数のレコードを持つことができます。
t_production_process_order_groups テーブルの 1 件のレコードは、t_production_process_orders テーブルの複数のレコードを持つことができます。
t_production_process_order_groups テーブルの 1 件のレコードは、w_productions テーブルの複数のレコードを持つことができます。

==== TProductionProcessOrderGroupRecord.php ====
t_production_process_order_group_records テーブルの 1 件のレコードは、t_production_process_order_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_order_group_records 側にあります）。
t_production_process_order_group_records テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_order_group_records 側にあります）。
t_production_process_order_group_records テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_order_group_records 側にあります）。

==== TProductionProcessOrderGroupRecordPauseTime.php ====
t_production_process_order_group_record_pause_times テーブルの 1 件のレコードは、t_production_process_order_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_order_group_record_pause_times 側にあります）。
t_production_process_order_group_record_pause_times テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_order_group_record_pause_times 側にあります）。
t_production_process_order_group_record_pause_times テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_process_order_group_record_pause_times 側にあります）。

==== TProductionReject.php ====
t_production_rejects テーブルの 1 件のレコードは、t_production_process_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_production_rejects 側にあります）。
t_production_rejects テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_rejects 側にあります）。
t_production_rejects テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_production_rejects 側にあります）。
t_production_rejects テーブルの 1 件のレコードは、created_bies テーブルの特定の 1 件のレコードに属します（外部キーは t_production_rejects 側にあります）。

==== TPurchaseOrder.php ====
t_purchase_orders テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_orders 側にあります）。
t_purchase_orders テーブルの 1 件のレコードは、m_suppliers テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_orders 側にあります）。
t_purchase_orders テーブルの 1 件のレコードは、t_estimates テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_orders 側にあります）。
t_purchase_orders テーブルの 1 件のレコードは、m_tax_rates テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_orders 側にあります）。
t_purchase_orders テーブルの 1 件のレコードは、m_purchase_order_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_orders 側にあります）。
t_purchase_orders テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_orders 側にあります）。
t_purchase_orders テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_orders 側にあります）。
t_purchase_orders テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_orders 側にあります）。
t_purchase_orders テーブルの 1 件のレコードは、allocations テーブルの 1 件のレコードを持ちます。
t_purchase_orders テーブルの 1 件のレコードは、t_purchase_order_details テーブルの複数のレコードを持つことができます。
t_purchase_orders テーブルの 1 件のレコードは、t_purchase_order_groups テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_orders 側にあります）。

==== TPurchaseOrderDetail.php ====
t_purchase_order_details テーブルの 1 件のレコードは、t_purchase_orders テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_details 側にあります）。
t_purchase_order_details テーブルの 1 件のレコードは、m_purchase_order_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_details 側にあります）。
t_purchase_order_details テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_details 側にあります）。
t_purchase_order_details テーブルの 1 件のレコードは、m_tax_rates テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_details 側にあります）。
t_purchase_order_details テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_details 側にあります）。
t_purchase_order_details テーブルの 1 件のレコードは、m_delivery_methods テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_details 側にあります）。
t_purchase_order_details テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_details 側にあります）。
t_purchase_order_details テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_details 側にあります）。

==== TPurchaseOrderGroup.php ====
t_purchase_order_groups テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_groups 側にあります）。
t_purchase_order_groups テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_purchase_order_groups 側にあります）。
t_purchase_order_groups テーブルの 1 件のレコードは、t_purchase_orders テーブルの複数のレコードを持つことができます。

==== TRackPartStock.php ====
t_rack_part_stocks テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stocks 側にあります）。
t_rack_part_stocks テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stocks 側にあります）。
t_rack_part_stocks テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stocks 側にあります）。
t_rack_part_stocks テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stocks 側にあります）。

==== TRackPartStockHistory.php ====
t_rack_part_stock_histories テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stock_histories 側にあります）。
t_rack_part_stock_histories テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stock_histories 側にあります）。
t_rack_part_stock_histories テーブルの 1 件のレコードは、m_stock_history_types テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stock_histories 側にあります）。
t_rack_part_stock_histories テーブルの 1 件のレコードは、m_stock_change_reasons テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stock_histories 側にあります）。
t_rack_part_stock_histories テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stock_histories 側にあります）。
t_rack_part_stock_histories テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stock_histories 側にあります）。
t_rack_part_stock_histories テーブルの 1 件のレコードは、checked_bies テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stock_histories 側にあります）。
t_rack_part_stock_histories テーブルの 1 件のレコードは、created_bies テーブルの特定の 1 件のレコードに属します（外部キーは t_rack_part_stock_histories 側にあります）。

==== TSalesPriceHistory.php ====
t_sales_price_histories テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_sales_price_histories 側にあります）。

==== TShipping.php ====
t_shippings テーブルの 1 件のレコードは、m_delivery_destinations テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。
t_shippings テーブルの 1 件のレコードは、ship_from_m_delivery_destinations テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。
t_shippings テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。
t_shippings テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。
t_shippings テーブルの 1 件のレコードは、m_shipping_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。
t_shippings テーブルの 1 件のレコードは、m_shipping_usages テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。
t_shippings テーブルの 1 件のレコードは、m_transport_method_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。
t_shippings テーブルの 1 件のレコードは、m_transport_companies テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。
t_shippings テーブルの 1 件のレコードは、m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。
t_shippings テーブルの 1 件のレコードは、t_charter_truck_requests テーブルの複数のレコードを持つことができます。
t_shippings テーブルの 1 件のレコードは、t_pickings テーブルの複数のレコードを持つことができます。
t_shippings テーブルの 1 件のレコードは、t_shipping_details テーブルの複数のレコードを持つことができます。
t_shippings テーブルの 1 件のレコードは、t_shipping_invoice_numbers テーブルの複数のレコードを持つことができます。
t_shippings テーブルの 1 件のレコードは、t_shipping_other_items テーブルの複数のレコードを持つことができます。
t_shippings テーブルの 1 件のレコードは、w_shippings テーブルの複数のレコードを持つことができます。
t_shippings テーブルの 1 件のレコードは、t_orders テーブルの特定の 1 件のレコードに属します（外部キーは t_shippings 側にあります）。

==== TShippingDetail.php ====
t_shipping_details テーブルの 1 件のレコードは、t_shippings テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_details 側にあります）。
t_shipping_details テーブルの 1 件のレコードは、t_order_details テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_details 側にあります）。
t_shipping_details テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_details 側にあります）。
t_shipping_details テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_details 側にあります）。
t_shipping_details テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_details 側にあります）。
t_shipping_details テーブルの 1 件のレコードは、m_production_numbers テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_details 側にあります）。
t_shipping_details テーブルの 1 件のレコードは、allocations テーブルの 1 件のレコードを持ちます。

==== TShippingInvoiceNumber.php ====
t_shipping_invoice_numbers テーブルの 1 件のレコードは、t_shippings テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_invoice_numbers 側にあります）。
t_shipping_invoice_numbers テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_invoice_numbers 側にあります）。
t_shipping_invoice_numbers テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_invoice_numbers 側にあります）。

==== TShippingOtherItem.php ====
t_shipping_other_items テーブルの 1 件のレコードは、t_shippings テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_other_items 側にあります）。
t_shipping_other_items テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_other_items 側にあります）。
t_shipping_other_items テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_other_items 側にあります）。

==== TShippingPickingPart.php ====
t_shipping_picking_parts テーブルの 1 件のレコードは、t_pickings テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_parts 側にあります）。
t_shipping_picking_parts テーブルの 1 件のレコードは、m_production_numbers テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_parts 側にあります）。
t_shipping_picking_parts テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_parts 側にあります）。
t_shipping_picking_parts テーブルの 1 件のレコードは、m_picking_statuses テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_parts 側にあります）。
t_shipping_picking_parts テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_parts 側にあります）。
t_shipping_picking_parts テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_parts 側にあります）。
t_shipping_picking_parts テーブルの 1 件のレコードは、t_picking_boxes テーブルの複数のレコードと多対多の関係を持ちます（中間テーブルを介します）。
t_shipping_picking_parts テーブルの 1 件のレコードは、t_shipping_picking_part_records テーブルの複数のレコードを持つことができます。

==== TShippingPickingPartRecord.php ====
t_shipping_picking_part_records テーブルの 1 件のレコードは、t_shipping_picking_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_part_records 側にあります）。
t_shipping_picking_part_records テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_part_records 側にあります）。
t_shipping_picking_part_records テーブルの 1 件のレコードは、created_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_part_records 側にあります）。
t_shipping_picking_part_records テーブルの 1 件のレコードは、updated_by_m_account_types テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_part_records 側にあります）。
t_shipping_picking_part_records テーブルの 1 件のレコードは、created_by_m_employees テーブルの特定の 1 件のレコードに属します（外部キーは t_shipping_picking_part_records 側にあります）。

==== TStandardStockUnitPriceHistory.php ====
t_standard_stock_unit_price_histories テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは t_standard_stock_unit_price_histories 側にあります）。

==== WArrival.php ====
w_arrivals テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは w_arrivals 側にあります）。
w_arrivals テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは w_arrivals 側にあります）。

==== WProduction.php ====
w_productions テーブルの 1 件のレコードは、t_production_process_order_groups テーブルの特定の 1 件のレコードに属します（外部キーは w_productions 側にあります）。

==== WProductionPicking.php ====
w_production_pickings テーブルの 1 件のレコードは、t_production_process_groups テーブルの特定の 1 件のレコードに属します（外部キーは w_production_pickings 側にあります）。
w_production_pickings テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは w_production_pickings 側にあります）。
w_production_pickings テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは w_production_pickings 側にあります）。

==== WShipping.php ====
w_shippings テーブルの 1 件のレコードは、t_shippings テーブルの特定の 1 件のレコードに属します（外部キーは w_shippings 側にあります）。

==== WShippingPicking.php ====
w_shipping_pickings テーブルの 1 件のレコードは、m_racks テーブルの特定の 1 件のレコードに属します（外部キーは w_shipping_pickings 側にあります）。
w_shipping_pickings テーブルの 1 件のレコードは、m_parts テーブルの特定の 1 件のレコードに属します（外部キーは w_shipping_pickings 側にあります）。

